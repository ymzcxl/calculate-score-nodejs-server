# 算分小程序后端详细部署文档

## 1. 部署前准备

### 1.1 服务器配置
- **推荐配置**：1-2核 CPU，1-2GB 内存，20GB 存储空间
- **操作系统**：Ubuntu 20.04 LTS 或 Ubuntu 22.04 LTS
- **网络**：确保服务器可以正常访问互联网
- **SSH工具**：FinalShell 或其他 SSH 客户端

### 1.2 本地环境准备
- **前端代码**：`d:\算分小程序\scoring` 目录
- **后端代码**：`d:\算分小程序\backend` 目录
- **SCP工具**：Windows 终端或其他 SCP 客户端

## 2. 详细部署步骤

### 2.1 连接服务器
```bash
# 使用 FinalShell 连接服务器
# 主机：38.182.96.171
# 端口：22
# 用户名：root
# 密码：您的服务器密码
```

### 2.2 系统初始化
```bash
# 更新系统包
apt update && apt upgrade -y

# 安装必要的工具
apt install -y curl wget git
```

### 2.3 安装 Node.js
```bash
# 导入 Node.js GPG 密钥
curl -fsSL https://deb.nodesource.com/gpgkey/nodesource.gpg.key | sudo gpg --dearmor -o /usr/share/keyrings/nodesource.gpg

# 添加 Node.js 14 源
echo "deb [signed-by=/usr/share/keyrings/nodesource.gpg] https://deb.nodesource.com/node_14.x nodistro main" | sudo tee /etc/apt/sources.list.d/nodesource.list

# 更新包列表
apt update

# 安装 Node.js 和 npm
apt install -y nodejs npm

# 验证安装
node -v
npm -v
```

### 2.4 安装 MongoDB
```bash
# 导入 MongoDB GPG 密钥
curl -fsSL https://www.mongodb.org/static/pgp/server-6.0.asc | sudo gpg --dearmor -o /usr/share/keyrings/mongodb-server-6.0.gpg

# 添加 MongoDB 源
echo "deb [ arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-6.0.gpg ] https://repo.mongodb.org/apt/ubuntu jammy/mongodb-org/6.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-6.0.list

# 更新包列表
apt update

# 安装 MongoDB
apt install -y mongodb-org

# 启动 MongoDB 服务
systemctl start mongod

# 设置 MongoDB 开机自启
systemctl enable mongod

# 验证 MongoDB 状态
systemctl status mongod
```

### 2.5 上传后端代码
```bash
# 在本地 Windows 终端执行
# 注意：使用绝对路径，路径中的空格需要用引号包围
scp -r "d:\算分小程序\backend" root@38.182.96.171:/var/www/
```

### 2.6 安装项目依赖
```bash
# 进入项目目录
cd /var/www/backend

# 安装项目依赖
npm install

# 安装 PM2 进程管理工具
npm install -g pm2
```

### 2.7 配置环境变量
```bash
# 查看 .env 文件配置
cat .env

# 确保配置正确
# MongoDB 配置：MONGODB_URI=mongodb://localhost:27017/scoring
# 服务器端口：PORT=3000
```

### 2.8 启动后端服务
```bash
# 使用 PM2 启动服务
pm start

# 查看服务状态
npm run status

# 设置 PM2 开机自启
npm run startup
```

### 2.9 配置防火墙
```bash
# 允许 3000 端口
ufw allow 3000

# 启用防火墙
ufw enable

# 查看防火墙状态
ufw status
```

## 3. 常见失败步骤及解决方案

### 3.1 SCP 上传失败
- **失败现象**：FinalShell 上传时只有文件夹，文件上传失败
- **失败原因**：
  - 权限问题：`/var/www/` 目录权限不足
  - 文件权限：本地文件权限限制
  - 网络问题：上传过程中网络不稳定
- **解决方案**：
  - 使用 SCP 命令上传：`scp -r "d:\算分小程序\backend" root@38.182.96.171:/var/www/`
  - 检查目录权限：`sudo chmod 755 /var/www/`
  - 分批上传文件

### 3.2 MongoDB 安装失败
- **失败现象**：安装 MongoDB 时出现依赖错误
- **失败原因**：
  - Ubuntu 版本与 MongoDB 版本不兼容
  - 缺少依赖包
- **解决方案**：
  - 使用兼容的 MongoDB 版本（Ubuntu 24.04 推荐 MongoDB 6.0）
  - 按照文档中的命令正确添加源

### 3.3 服务启动失败
- **失败现象**：服务启动后状态显示错误
- **失败原因**：
  - 端口占用
  - MongoDB 连接失败
  - 依赖安装不完整
- **解决方案**：
  - 检查端口：`netstat -tuln | grep 3000`
  - 检查 MongoDB 状态：`systemctl status mongod`
  - 重新安装依赖：`npm install`

### 3.4 外部访问失败
- **失败现象**：无法通过浏览器访问服务
- **失败原因**：
  - 防火墙未配置
  - 服务器安全组未开放端口
  - 服务未运行
- **解决方案**：
  - 配置防火墙：`ufw allow 3000`
  - 检查服务状态：`pm2 status`
  - 检查安全组配置（云服务器）

## 4. 正确命令输入方式

### 4.1 SCP 命令
```bash
# 正确格式
scp -r "本地目录" 用户名@服务器IP:目标目录

# 示例
scp -r "d:\算分小程序\backend" root@38.182.96.171:/var/www/
```

### 4.2 PM2 命令
```bash
# 启动服务
pm start

# 查看状态
npm run status

# 查看日志
npm run logs

# 重启服务
npm run restart

# 停止服务
npm run stop
```

### 4.3 MongoDB 命令
```bash
# 启动服务
systemctl start mongod

# 停止服务
systemctl stop mongod

# 重启服务
systemctl restart mongod

# 查看状态
systemctl status mongod
```

### 4.4 防火墙命令
```bash
# 允许端口
ufw allow 3000

# 启用防火墙
ufw enable

# 查看状态
ufw status
```

## 5. 测试和验证

### 5.1 本地测试
```bash
# 测试本地连接
curl http://localhost:3000

# 查看服务日志
npm run logs
```

### 5.2 外部访问测试
- 在浏览器中访问：`http://38.182.96.171:3000`
- 预期结果：看到服务器运行信息

### 5.3 前端连接测试
- 打开微信开发者工具
- 导入 `d:\算分小程序\scoring` 项目
- 测试登录、创建房间等功能
- 预期结果：所有功能正常运行

## 6. 前端配置

### 6.1 API 地址配置
确保前端代码中的 API 地址已更新为服务器地址：
- **登录页面**：`const API_BASE_URL = 'http://38.182.96.171:3000/api';`
- **首页**：`const API_BASE_URL = 'http://38.182.96.171:3000/api';`
- **房间页面**：`const API_BASE_URL = 'http://38.182.96.171:3000/api';` 和 `const SOCKET_URL = 'http://38.182.96.171:3000';`
- **历史记录页面**：`const API_BASE_URL = 'http://38.182.96.171:3000/api';`

### 6.2 微信小程序配置
- 在微信小程序管理后台配置服务器域名：`38.182.96.171`
- 测试环境可在微信开发者工具中开启 "不校验合法域名" 选项

## 7. 维护和监控

### 7.1 服务管理
- **启动服务**：`npm start`
- **停止服务**：`npm run stop`
- **重启服务**：`npm run restart`
- **查看日志**：`npm run logs`
- **查看状态**：`npm run status`

### 7.2 系统监控
- **查看系统状态**：`top`
- **查看网络状态**：`netstat -tuln`
- **查看磁盘使用**：`df -h`
- **查看 MongoDB 状态**：`systemctl status mongod`

## 8. 部署完成确认

当完成以上步骤后，您的算分小程序后端服务应该已经成功部署并运行。前端小程序可以通过配置的 API 地址连接到后端，实现完整的功能。

### 验证清单
- [ ] 服务器环境搭建完成
- [ ] MongoDB 服务运行正常
- [ ] 后端服务启动成功
- [ ] 防火墙配置正确
- [ ] 外部访问测试通过
- [ ] 前端连接测试通过
- [ ] 所有功能正常运行

## 9. 故障排查

### 9.1 服务无法启动
1. 检查 MongoDB 服务状态
2. 检查端口是否被占用
3. 检查依赖是否安装完整
4. 查看服务日志获取详细错误信息

### 9.2 数据库连接失败
1. 检查 MongoDB 服务是否运行
2. 检查 MongoDB 配置是否正确
3. 检查数据库权限
4. 重启 MongoDB 服务

### 9.3 前端无法连接
1. 检查 API 地址配置是否正确
2. 检查服务器防火墙配置
3. 检查服务是否运行
4. 检查网络连接

## 10. 总结

本部署文档详细介绍了算分小程序后端服务的部署过程，包括环境搭建、代码部署、服务启动、测试验证等步骤。同时，针对部署过程中可能遇到的问题提供了详细的解决方案，确保部署过程顺利完成。

通过本文档的指导，您应该能够成功部署算分小程序后端服务，并与前端小程序进行对接，实现完整的功能。