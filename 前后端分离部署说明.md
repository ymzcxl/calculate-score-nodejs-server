# 算分项目后端部署说明

## 当前架构

- `backend` 只负责后端 API 和 Socket 实时服务
- `scoring` 是唯一前端源码目录
- 后端不再托管任何前端静态页面

## 后端职责

- REST API：`/api/auth`、`/api/user`、`/api/room`、`/api/score`、`/api/message`、`/api/history`
- 健康检查：`/health`
- Socket.io：与前端实时房间同步共用同一个后端服务地址

## 环境变量

参考 [`.env.example`](file:///d:/%E7%AE%97%E5%88%86%E5%B0%8F%E7%A8%8B%E5%BA%8F/backend/.env.example)：

```env
PORT=3000
CORS_ORIGIN=*
MONGODB_URI=mongodb://localhost:27017/scoring
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=7d
WECHAT_APPID=your_wechat_appid
WECHAT_SECRET=your_wechat_secret
```

`CORS_ORIGIN` 说明：

- 本地联调可先用 `*`
- 正式环境建议填前端站点地址，多个域名用英文逗号分隔
- 示例：`https://score.example.com,https://admin.example.com`

## 启动方式

```bash
npm install
npm start
```

开发模式：

```bash
npm run dev
```

## 部署建议

1. 把 `backend` 单独部署到 Node 运行环境
2. 配置好 `.env`
3. 启动 `node src/app.js`
4. 对外开放后端端口，或通过 Nginx 反向代理

## 验证方法

启动后访问：

```bash
curl http://127.0.0.1:3000/health
```

预期返回：

```json
{
  "code": 200,
  "message": "ok"
}
```

访问根路径：

```bash
curl http://127.0.0.1:3000/
```

应返回 JSON 服务信息，而不是前端页面。这表示后端已经是纯服务形态。

## 前端部署

前端请查看 [scoring/H5前端部署说明.md](file:///d:/%E7%AE%97%E5%88%86%E5%B0%8F%E7%A8%8B%E5%BA%8F/scoring/H5%E5%89%8D%E7%AB%AF%E9%83%A8%E7%BD%B2%E8%AF%B4%E6%98%8E.md)。
