# 算分项目后端更新说明

## 1. 后端目录

后端源码目录：

`d:\算分小程序\backend`

服务器部署目录：

`/www/wwwroot/score-web-app`

## 2. 服务器必须保留的内容

后端更新时，服务器上的下面两个内容不要删除：

```bash
.env
node_modules
```

原因：

- `.env` 里是线上端口、数据库、JWT 等配置
- `node_modules` 是服务器当前可运行的依赖

## 3. 当前线上 `.env` 关键配置

线上至少要有这些：

```env
PORT=9999
MONGODB_URI=mongodb://localhost:27017/scoring
JWT_SECRET=scoring_app_secret_key_2024
JWT_EXPIRES_IN=7d
WECHAT_APPID=test_appid
WECHAT_SECRET=test_secret
SERVER_IP=38.182.96.171
SERVER_PORT=9999
```

如果后端启动后跑到 `3000`，说明没有正确吃到这个配置。

## 4. 本地打包方式

如果需要压缩成部署包，可在项目根目录执行：

```powershell
$backendItems = Get-ChildItem "d:\算分小程序\backend" -Force | Where-Object { $_.Name -notin @('node_modules','.env') }
Compress-Archive -Path $backendItems.FullName -DestinationPath "d:\算分小程序\后端H5.zip" -Force
```

说明：

- 打包时不要把本地 `.env` 带进去
- 也不要把本地 `node_modules` 打进去

## 5. 服务器更新步骤

推荐步骤：

1. 把 `后端H5.zip` 上传到 `/www/wwwroot/score-web-app`
2. 解压
3. 覆盖旧代码
4. 确认 `.env` 还在
5. 确认 `node_modules` 还在

更新后建议先检查：

```bash
cd /www/wwwroot/score-web-app
ls -la
cat .env
```

## 6. 正确启动命令

这套项目后端建议固定用下面这条命令启动：

```bash
cd /www/wwwroot/score-web-app
PORT=9999 nohup node src/app.js > app.log 2>&1 &
```

注意：

- 必须在项目根目录执行
- 不建议裸跑 `nohup node src/app.js`
- 这次实际排查过，显式带 `PORT=9999` 最稳

## 7. 更新后怎么验证

启动后执行：

```bash
curl http://127.0.0.1:9999/health
```

正常应返回：

```json
{"code":200,"message":"ok","data":{"status":"healthy","database":1}}
```

## 8. 常用排查命令

查看日志：

```bash
tail -n 100 /www/wwwroot/score-web-app/app.log
```

查看端口占用：

```bash
lsof -i :9999
lsof -i :3000
```

如果旧进程占端口，先杀掉：

```bash
kill -9 PID
```

如果依赖丢失，再执行：

```bash
cd /www/wwwroot/score-web-app
npm install
```

## 9. 推荐更新顺序

如果前后端都改了，推荐这样发：

1. 先更新后端
2. 启动后端并确认 `/health` 正常
3. 再更新前端静态文件
4. 最后测试创建房间、进房、消息、计分

## 10. 对应前端文档

前端更新说明见：

`d:\算分小程序\scoring\H5前端部署说明.md`
