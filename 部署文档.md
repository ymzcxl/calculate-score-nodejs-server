# 算分小程序后端部署文档

## 1. 服务器准备

### 1.1 服务器配置
- **推荐配置**：1-2核 CPU，1-2GB 内存，20GB 存储空间
- **操作系统**：Ubuntu 20.04 LTS 或 Ubuntu 22.04 LTS

### 1.2 连接服务器
使用 FinalShell 或其他 SSH 工具连接服务器：
```bash
ssh root@38.182.96.171
```

## 2. 环境安装

### 2.1 安装 Node.js
```bash
# 更新系统包
apt update && apt upgrade -y

# 安装 Node.js 14
curl -fsSL https://deb.nodesource.com/gpgkey/nodesource.gpg.key | sudo gpg --dearmor -o /usr/share/keyrings/nodesource.gpg
echo "deb [signed-by=/usr/share/keyrings/nodesource.gpg] https://deb.nodesource.com/node_14.x nodistro main" | sudo tee /etc/apt/sources.list.d/nodesource.list
apt update
apt install -y nodejs npm

# 验证安装
node -v
npm -v
```

### 2.2 安装 MongoDB
```bash
# 导入 MongoDB GPG 密钥
curl -fsSL https://www.mongodb.org/static/pgp/server-6.0.asc | sudo gpg --dearmor -o /usr/share/keyrings/mongodb-server-6.0.gpg

# 添加 MongoDB 源
echo "deb [ arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-6.0.gpg ] https://repo.mongodb.org/apt/ubuntu jammy/mongodb-org/6.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-6.0.list

# 安装 MongoDB
apt update
apt install -y mongodb-org

# 启动 MongoDB 服务
systemctl start mongod
systemctl enable mongod

# 验证 MongoDB 状态
systemctl status mongod
```

## 3. 代码部署

### 3.1 上传代码
使用 SCP 命令上传后端代码：
```bash
# 在本地 Windows 终端执行
scp -r "d:\算分小程序\backend" root@38.182.96.171:/var/www/
```

### 3.2 安装依赖
```bash
# 进入项目目录
cd /var/www/backend

# 安装项目依赖
npm install

# 安装 PM2 进程管理工具
npm install -g pm2
```

### 3.3 配置环境变量
检查 `.env` 文件配置：
```bash
# 查看配置
cat .env

# 确保配置正确
# MongoDB 配置：MONGODB_URI=mongodb://localhost:27017/scoring
# 服务器端口：PORT=3000
# JWT 密钥：JWT_SECRET=scoring_app_secret_key_2024
```

## 4. 服务启动

### 4.1 启动后端服务
```bash
# 使用 PM2 启动服务
npm start

# 查看服务状态
npm run status

# 设置 PM2 开机自启
npm run startup
```

### 4.2 配置防火墙
```bash
# 允许 3000 端口
ufw allow 3000

# 启用防火墙
ufw enable

# 查看防火墙状态
ufw status
```

## 5. 测试验证

### 5.1 本地测试
```bash
# 测试本地连接
curl http://localhost:3000

# 查看服务日志
npm run logs
```

### 5.2 外部访问测试
在浏览器中访问：`http://38.182.96.171:3000`

## 6. 常见问题及解决方案

### 6.1 MongoDB 连接失败
- **问题**：服务启动后日志显示 "MongoDB连接失败"
- **解决方案**：
  - 检查 MongoDB 服务是否运行：`systemctl status mongod`
  - 重启 MongoDB 服务：`systemctl restart mongod`
  - 检查 MongoDB 配置是否正确

### 6.2 端口占用
- **问题**：启动服务时显示 "EADDRINUSE: address already in use"
- **解决方案**：
  - 查找占用端口的进程：`netstat -ano | findstr :3000`
  - 终止占用端口的进程：`taskkill /F /PID <进程ID>`

### 6.3 防火墙问题
- **问题**：外部无法访问服务
- **解决方案**：
  - 检查防火墙状态：`ufw status`
  - 确保 3000 端口已开放：`ufw allow 3000`
  - 检查服务器安全组配置（如果是云服务器）

### 6.4 依赖安装失败
- **问题**：`npm install` 失败
- **解决方案**：
  - 检查网络连接
  - 清理 npm 缓存：`npm cache clean --force`
  - 重新安装依赖：`npm install`

### 6.5 注册失败
- **问题**：注册时返回 500 错误
- **解决方案**：
  - 检查 MongoDB 连接状态
  - 查看服务日志：`npm run logs`
  - 检查用户模型的唯一索引设置

## 7. 前端配置

### 7.1 环境配置
前端已实现环境区分功能，通过 `config/env.js` 文件管理不同环境的 API 地址：

**开发环境**：
- API 地址：`http://localhost:3000/api`

**生产环境**：
- API 地址：`http://38.182.96.171:3000/api`

### 7.2 微信小程序配置
- 在微信小程序管理后台配置服务器域名：`38.182.96.171`
- 测试环境可在微信开发者工具中开启 "不校验合法域名" 选项

## 8. 功能说明

### 8.1 注册功能
- 支持手机号注册
- 密码加密存储
- 手机号唯一性验证
- 表单验证（手机号格式、密码长度、昵称长度）

### 8.2 登录功能
- 支持手机号密码登录
- 支持微信登录
- JWT 令牌认证

### 8.3 退出功能
- 前端清除本地存储的用户信息和令牌
- 后端记录退出日志

### 8.4 其他功能
- 用户信息修改（昵称、头像）
- 游戏统计数据展示
- 房间创建和加入

## 9. 维护命令

### 9.1 服务管理
- **启动服务**：`npm start`
- **停止服务**：`npm run stop`
- **重启服务**：`npm run restart`
- **查看日志**：`npm run logs`
- **查看状态**：`npm run status`

### 9.2 MongoDB 管理
- **启动 MongoDB**：`systemctl start mongod`
- **停止 MongoDB**：`systemctl stop mongod`
- **重启 MongoDB**：`systemctl restart mongod`

### 9.3 系统管理
- **查看系统状态**：`top`
- **查看网络状态**：`netstat -tuln`
- **查看磁盘使用**：`df -h`

## 10. 部署完成确认

当完成以上步骤后，您的算分小程序后端服务应该已经成功部署并运行。前端小程序可以通过配置的 API 地址连接到后端，实现完整的功能。

### 10.1 功能测试
1. **注册测试**：使用新手机号注册账号
2. **登录测试**：使用注册的账号登录
3. **微信登录测试**：使用微信账号登录
4. **退出测试**：点击退出登录按钮
5. **用户信息修改测试**：修改昵称和头像

### 10.2 环境切换测试
- **本地开发**：在微信开发者工具中运行，使用本地 API 地址
- **线上环境**：发布小程序后，使用线上 API 地址

如果遇到任何问题，请参考 "常见问题及解决方案" 部分，或检查服务日志获取详细错误信息。